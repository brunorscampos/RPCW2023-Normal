extends layout

block content
  .w3-card-4
    header.w3-container.w3-blue
      h1 
        i.fa.fa-home.clickable(onclick="window.location='/'" style='cursor:pointer;') 
        | #{contract._id}
    .w3-container
      each val, key in contract
        p
          b #{key}:
          |  #{val}
    footer.w3-container.w3-blue.w3-center
      h5 Generated for RPCW2023 in #{d}